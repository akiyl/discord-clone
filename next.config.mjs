/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
          domains: [
               "uploadingthing.com",
               "utfs.io"
          ]
     }
};

export default nextConfig;
